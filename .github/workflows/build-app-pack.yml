on:
  push:
    branches:
      - mlucas/nasa-ogc

jobs:
  build_app_pack:
    environment: DIT
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo content
        uses: actions/checkout@v4

      - uses: .github/actions/